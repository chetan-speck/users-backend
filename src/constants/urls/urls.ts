// ------------------------------------------------------------------------------------------

export const rootUrl = {
	getRoot: '/',
};

// ------------------------------------------------------------------------------------------

export const notFoundUrl = {
	notFound: '*',
};

// ------------------------------------------------------------------------------------------

export const userUrl = {
	createUser: '/api/users',
	signInUser: '/api/users/signin',
	signOutUser: '/api/users/signout',
	checkEmailExists: '/api/users/check-email',
	getSignUpToken: '/api/users/signup-token',
	getCurrentUser: '/api/users/me',
};

// ------------------------------------------------------------------------------------------

export const filesUrl = {
	uploadFile: '/api/files',
};

// ------------------------------------------------------------------------------------------
